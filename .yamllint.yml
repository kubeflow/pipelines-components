---
extends: default
ignore: |
  .tekton/
  # KFP-compiled IR: list indentation and long embedded strings do not match yamllint rules.
  pipelines/**/pipeline.yaml
rules:
  line-length:
    max: 120
    level: warning
  truthy:
    level: warning
    ignore: |
      .github/workflows
