---
name: Component/Pipeline Tests (Placeholder)

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: Placeholder (manual-only)
    runs-on: ubuntu-24.04
    steps:
      - name: Placeholder
        run: |
          echo "TODO: Implement component/pipeline unit + LocalRunner test enforcement."
          echo "This workflow is intentionally manual-only (workflow_dispatch) until implemented."
